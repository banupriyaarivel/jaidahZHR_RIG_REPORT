# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.5"
metadata:
  name: jaidahZHR_RIG_REPORT2
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://10.10.1.30:8000
          client: '900'
        app:
          name: ZHR_RIG_REPORT2
          description: Allowance Report Change request
          package: ZFIORI
          transport: E03K929440
        exclude:
          - /test/
